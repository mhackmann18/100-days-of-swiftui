import Cocoa

var gameOver = false

// Flips a boolean's value
gameOver.toggle()

// You could use an addition operator to add strings together, but that can be inneficient...
// Instead use STRING INTERPOLATION (it works with numbers too)

// example

let name = "Bobby"

let age = 26

let intro = "Hi, my name is \(name), and I'm \(age) years old"

print(intro)

